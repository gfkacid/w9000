import { Header } from "@/components";
import styles from "./styles.module.scss";

const Home = () => {
  return (
    <div className={styles.main}>
      <Header />
    </div>
  );
};

export default Home;
